import styled from 'styled-components';

export const Component = styled.span`
  width: 100%;
  height: 4px;
  max-width: 360px;
  opacity: 0.3;
  margin: 1rem;
  background: radial-gradient(50% 50% at 50% 50%, var(--Dark-50) 0%, rgba(152, 115, 255, 0) 100%);
`;
